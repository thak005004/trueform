node_modules/
.next/
/out/
/build
/coverage
.env
.env.local
.env*.local
.vercel
*.log
*.tsbuildinfo
next-env.d.ts
.DS_Store

# internal build brief — not part of the deliverable
CLAUDE_CODE_BRIEF.md

# private writeup — sent separately, not in the public repo
WRITEUP.md

# stray Tesseract training data (downloaded from CDN at runtime, never needed in-repo)
eng.traineddata
*.traineddata

# internal build-continuity doc — not part of the deliverable
HANDOFF.md

# personal notes on how I used AI — private, not in the public repo
AI-NOTES*.md
.gstack/
